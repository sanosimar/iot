<h1>Login</h1>
<h2 class="text-muted"><small>Choose one of the following login methods.</small></h2>

Facebook Google twitter
<hr>
<h2 class="text-muted"><small>Or login with your email address.</small></h2>
<%= form_tag({controller: "sessions", action: "create"}, method: "post") do %>
    <div class="form-group">
      <%= label_tag(name="email") %>
      <%= email_field_tag(name = "email", value = nil, options = { class: "form-control"}) %>
    </div>
    <div class="form-group">
      <%= label_tag(name="password") %>
      <%= password_field_tag(name = "password", value = nil, options = { class: "form-control"}) %>
    </div>
    <%= submit_tag "Login", class: "btn btn-success" %>
<% end %>