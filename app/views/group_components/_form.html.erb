<%= form_for(@group_component) do |f| %>
  <% if @group_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_component.errors.count, "error") %> prohibited this group_component from being saved:</h2>

      <ul>
      <% @group_component.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :enable %><br>
    <%= f.check_box :enable, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
